import { SwaggerConfig } from './swagger.interface';
export const SWAGGER_CONFIG: SwaggerConfig = {
  title: "Emediong's Portfolio Template",
  description: 'My first build of my new generation portfolio website',
  version: '1.00',
  tags: ['template'],
};
